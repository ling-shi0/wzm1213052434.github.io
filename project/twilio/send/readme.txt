命令行中分别执行以下语句：
pip install json
pip install requests
pip install datetime

复制文件至预定地点
分别打开四个文件，更改其程序头的绝对路径
在文件夹中建立oldfile.txt文件

设置环境变量

打开send.bat

read.py或者read.bat是提供给前端程序的接口，前端调用
这两个文件中的任意一个即可。

json文件保存名为:newfile.json

提供一组测试用例，test.txt，test.json可供本地测试。